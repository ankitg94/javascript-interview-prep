//setTimeout

/*setTimeout is a fundamental function in JavaScript for scheduling
 code to run asynchronously after a specified delay. 
It’s commonly used for handling animations,
 delaying actions, or scheduling periodic tasks. */
/*
let abc= setTimeout(()=>{
   console.log("this is called after 5 sec")
 },5000)
 //canceling the timeout
 //clearTimeout(abc)
 console.log(abc)
 */

 /*
 setInterval is a powerful JavaScript function for executing code
  repeatedly at fixed intervals. It’s commonly used for real-time 
  updates, animations, polling, and other periodic tasks in web 
  applications. Understanding how to use setInterval effectively 
  can enhance the interactivity and responsiveness of your JavaScript
   applications. 
   */
  
  /*
   const intervalId = setInterval(function() {
    console.log('Executing setInterval...');
}, 2000);

// Stop the interval after 10 seconds
setTimeout(function() {
    clearInterval(intervalId);
    console.log('Interval stopped.');
}, 10000);


*/

