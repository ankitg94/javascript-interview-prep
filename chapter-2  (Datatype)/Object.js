//object is datatype which is used to store data in key-value form 
//indexing is not required like array 
const abc={
    name:"ankit",
    class:12
}
console.log(abc)
console.log(abc.name)

//object freeze object lock hojayega (object.freeze(jsUser))